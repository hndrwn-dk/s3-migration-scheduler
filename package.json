{
  "name": "s3-migration-dashboard",
  "version": "1.0.0",
  "description": "Fullstack S3 bucket migration dashboard with MinIO client",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "dev:stable": "concurrently \"npm run server:start\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "server:start": "cd server && npm start",
    "client:dev": "cd client && npm start",
    "server:install": "cd server && npm install",
    "client:install": "cd client && npm install",
    "install:all": "npm run server:install && npm run client:install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "keywords": ["s3", "migration", "minio", "dashboard", "fullstack"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}